#include <iostream>
#include<vector>
#include <algorithm>  // for sort
using namespace std;

int SumArr(int arr[], int n) {
    // basecase
    
}

int main() {
    int arr[] = {5, 2, 9, 1, 5, 6};
    int n = sizeof(arr)/sizeof(arr[0]);

    SumArr(arr, n);
    return 0;
}
